package services

func PasswordAuthorizationHandler(username, password string) (userID string, err error){
	//ctx, cancel := context.WithTimeout(context.Background(), time.Second * 5)
	//defer  cancel()
	//
	//return LoginUser(ctx, username, password)

	return "tes", nil
}